{
  "name": "hitobit-store",
  "version": "0.2.1",
  "sideEffects": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc -p ./tsconfig.json",
    "build:typings": "tsc -p ./tsconfig.json --declarationDir dist"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "hitobit-services": "^0.2.1",
    "react-principal": "^2.0.7"
  }
}