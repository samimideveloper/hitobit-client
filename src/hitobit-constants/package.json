{
  "name": "hitobit-constants",
  "version": "0.2.2",
  "sideEffects": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc",
    "build:typings": "tsc -p ./tsconfig.json --declarationDir dist"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "buffer": "^6.0.3"
  }
}
