{
  "name": "hitobit-client",
  "version": "0.0.22",
  "sideEffects": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc -p ./tsconfig.json",
    "build:typings": "tsc -p ./tsconfig.json --declarationDir dist"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "hitobit-hooks": "^0.0.22",
    "hitobit-modules": "^0.0.22",
    "hitobit-services": "^0.0.22",
    "hitobit-store": "^0.0.22",
    "hitobit-utils": "^0.0.22"
  }
}
