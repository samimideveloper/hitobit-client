{
  "name": "hitobit-client",
  "version": "0.1.1",
  "sideEffects": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc -p ./tsconfig.json",
    "build:typings": "tsc -p ./tsconfig.json --declarationDir dist"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "hitobit-hooks": "^0.1.1",
    "hitobit-modules": "^0.1.0",
    "hitobit-services": "^0.1.0",
    "hitobit-store": "^0.1.0",
    "hitobit-utils": "^0.1.0"
  }
}
