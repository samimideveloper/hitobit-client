{
  "name": "hitobit-client",
  "version": "0.0.21",
  "sideEffects": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc -p ./tsconfig.json",
    "build:typings": "tsc -p ./tsconfig.json --declarationDir dist"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "hitobit-hooks": "^0.0.21",
    "hitobit-modules": "^0.0.21",
    "hitobit-services": "^0.0.21",
    "hitobit-store": "^0.0.21",
    "hitobit-utils": "^0.0.21"
  }
}
