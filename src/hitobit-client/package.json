{
  "name": "hitobit-client",
  "version": "0.2.0",
  "sideEffects": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc -p ./tsconfig.json",
    "build:typings": "tsc -p ./tsconfig.json --declarationDir dist"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "hitobit-hooks": "^0.2.0",
    "hitobit-modules": "^0.2.0",
    "hitobit-services": "^0.2.0",
    "hitobit-store": "^0.2.0",
    "hitobit-utils": "^0.2.0"
  }
}
