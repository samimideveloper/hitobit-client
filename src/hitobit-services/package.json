{
  "name": "hitobit-services",
  "version": "0.0.23",
  "sideEffects": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist && tsc -p ./tsconfig.json",
    "build:typings": "tsc -p ./tsconfig.json --declarationDir dist"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "axios": "^0.27.2",
    "buffer": "^6.0.3",
    "hitobit-constants": "^0.0.23",
    "hitobit-modules": "^0.0.23"
  }
}
